#pragma once

#include "ofMain.h"
#include <math.h>

class Menu{
	public:
		// We declare the basic methods for the class.
		Menu();
		~Menu();
		void setup();
		void update(float dt);
		void draw();


	private:

};
